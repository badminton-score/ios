# 更新日志

## 2.1

- **对战记录支持批量删除** —— 左上角「选择」进多选模式，每行左边出现系统圆圈；
  选择模式下左上角是「全选 / 取消全选」，右上角是「删除 n」（没选中时是灰的）。
  原来那个单独的「清空」按钮去掉了，全选再删就等于清空。
- **单条记录向左滑**，右侧露出红色删除，滑到底可以直接删。
- 记录页从 `ScrollView` 换成原生 `List` —— 只有 `List` 才有系统原生的滑动删除和多选圆圈。
- **修：对战记录一条都存不下来**。历史仓库是 `weak var` 靠外部注入，而注入那行代码没写进去，
  于是 `history?.add(...)` 一直是空操作。现在改成默认指向共享实例，结构上不可能漏。
- 测试 45 → 51 项，新增 6 项对战记录测试。

## 2.0

第一个公开版本。相对最初的 1.0：

- **自定义赛制** —— 自己定每局几分（5–50）、要不要封顶、打几局（1/3/5）
- **单打 / 双打** —— 双打每方两人，发球按 BWF 轮转
- **对战记录** —— 打完自动存一条
- **六种计分模式** 全部内置
- **重画 App 图标** —— 完整的羽毛球 + 平滑红蓝光晕
- 补上 git 版本控制、`Tools/verify.sh`、`Tools/make_ipa.sh`、`Tools/make_icon.swift`
- 修 `MatchStore.redo()` 闪退（撤销后点重做必崩）

## 1.0

最初的版本。

- 21 / 15 / 11 分制、一局 21 分
- 点面板加分，点减号撤回上一次加分
- 到分自动判胜
- 撤销 / 重做
- 逐分记录
- 中途退出不怕丢
